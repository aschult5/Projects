#ifndef SEQUENCE_HPP
#define SEQUENCE_HPP

#include <vector>

unsigned int binSearch(std::vector<int> const& vec, int v, unsigned int l, unsigned int r);
int longestConsecutiveSequence(std::vector<int> const& vec);

#endif
